import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'PLAY — G DEV. F.C.' };

export default function PlayLayout({ children }: { children: React.ReactNode }) {
  return children;
}
